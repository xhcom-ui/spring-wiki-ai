version: '3.8'

services:
  mayfly-go:
    image: mayfly-go/mayfly:latest
    container_name: mayfly-go
    restart: always
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    networks:
      - mayfly-network

networks:
  mayfly-network:
    driver: bridge
