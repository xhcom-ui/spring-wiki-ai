version: '3.8'

services:
  datagear:
    image: datagear/datagear:latest
    container_name: datagear
    restart: always
    ports:
      - "5040:5040"
    volumes:
      - ./data:/opt/datagear/data
    networks:
      - datagear-network

networks:
  datagear-network:
    driver: bridge
