version: '3.8'

services:
  chatgpt-web:
    image: chatgpt-web:latest
    container_name: chatgpt-web
    restart: always
    ports:
      - "3000:3000"
    environment:
      - OPENAI_API_KEY=your_api_key
    networks:
      - chatgpt-network

networks:
  chatgpt-network:
    driver: bridge
