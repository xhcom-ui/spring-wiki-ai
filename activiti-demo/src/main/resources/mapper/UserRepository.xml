<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.activiti.repository.UserRepository">

    <resultMap id="UserResultMap" type="User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="UserColumns">id, username, password, nickname, email, phone, status, created_at, updated_at</sql>

    <select id="selectById" resultMap="UserResultMap">
        select <include refid="UserColumns"/>
        from sys_user
        where id = #{id}
    </select>

    <select id="findByUsername" resultMap="UserResultMap">
        select <include refid="UserColumns"/>
        from sys_user
        where username = #{username}
    </select>

    <select id="findAll" resultMap="UserResultMap">
        select <include refid="UserColumns"/>
        from sys_user
        order by id
    </select>

    <insert id="insert" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        insert into sys_user (username, password, nickname, email, phone, status, created_at, updated_at)
        values (#{username}, #{password}, #{nickname}, #{email}, #{phone}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="User">
        update sys_user
        set username = #{username},
            password = #{password},
            nickname = #{nickname},
            email = #{email},
            phone = #{phone},
            status = #{status},
            created_at = #{createdAt},
            updated_at = #{updatedAt}
        where id = #{id}
    </update>

    <delete id="removeById">
        delete from sys_user where id = #{id}
    </delete>
</mapper>
