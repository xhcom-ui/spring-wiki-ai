server:
  port: 9001

spring:
  application:
    name: resource-server
  redis:
    host: localhost
    port: 6379
    password: 
    database: 0

# OAuth2 资源服务器配置
spring.security.oauth2.resourceserver:
  jwt:
    issuer-uri: http://localhost:9000
