server:
  port: 8082

spring:
  application:
    name: opendataloader-demo
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# opendataloader 配置
opendataloader:
  python-path: python3
  script-path: scripts/parse_pdf.py
  output-dir: output
