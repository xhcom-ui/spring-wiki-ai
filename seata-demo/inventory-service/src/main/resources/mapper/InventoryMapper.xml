<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seata.demo.inventory.mapper.InventoryMapper">
    <update id="deductStock">
        UPDATE t_inventory SET stock = stock - #{count} WHERE product_id = #{productId} AND stock >= #{count}
    </update>
</mapper>
